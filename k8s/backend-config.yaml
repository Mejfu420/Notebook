apiVersion: cloud.google.com/v1
kind: BackendConfig
metadata:
  name: backend-health-config
  namespace: red-notes
spec:
  healthCheck:
    checkIntervalSec: 15
    timeoutSec: 5
    healthyThreshold: 1
    unhealthyThreshold: 2
    port: 8080
    type: HTTP
    requestPath: /api/health